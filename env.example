# 서버 포트 설정
PORT=3000

# 프로젝트 인증 키 (하드코딩된 키 값)
PROJECT_KEY=your-secretKey

# CORS 설정 (프론트엔드 URL, 여러 개는 쉼표로 구분)
# 예: CORS_ORIGINS=http://localhost:5173,http://localhost:3001,https://yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3001

# 로그 레벨 (DEBUG, INFO, WARN, ERROR)
LOG_LEVEL=INFO

